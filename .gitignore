.devcontainer/
# Carpeta de compilación y binarios (¡CRÍTICO!)
build/

# Archivos de configuración antiguos o temporales autogenerados
sdkconfig.old
*.swp
.DS_Store
.vscode/

# Componentes gestionados por el IDF Component Manager
# (Se descargan solos al compilar si están en el idf_component.yml)
managed_components/
dependencies.lock
secrets.h

.clangd